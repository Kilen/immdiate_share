<%= form_for(@comment, :url=>comments_path) do |f| %>
  content: 
  <br />
  <%= f.text_area(:content) %>
  <%= fields_for(@share_info, :index=>nil) do |share| %>
    <%= share.hidden_field(:id) %>
    <%= share.hidden_field(:from) %>
  <% end %>
  <br />
  <%= f.submit %>
<% end %>
